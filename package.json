{
  "name": "foodvigil-platform",
  "version": "1.0.0",
  "private": true,
  "description": "FoodVigil — AI Consumer Food Safety & Transparency Platform",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend && node -e \"const fs = require('fs'); fs.cpSync('frontend/dist', 'dist', {recursive: true}); console.log('Successfully copied dist output to root');\"",
    "dev": "npm run dev --workspace=frontend",
    "start": "npm start --workspace=backend"
  }
}
